export const PageTitle = ({title}: {title: string}) => (
  <div className="bg-gray-200 shadow">
    <h1 className="mx-auto max-w-7xl py-6 px-4 sm:px-6 lg:px-8 text-3xl font-bold tracking-tight text-gray-900">
      {title}
    </h1>
  </div>
)